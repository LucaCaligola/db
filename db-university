SELECT  * 
FROM `students` 
WHERE `surname` 
LIKE  `%o `;

SELECT * AVG(`vote`), `exam_id`
FROM `exam_student` 
GROUP BY `exam_id`
HAVING AVG(`vote`) > 28;

SELECT * 
FROM `teachers`
WHERE `name`
LIKE `%s% ` AND `office_number` > 96;

